apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  # The name is generated by the PaC controller.
  # The 'on-event' annotation tells PaC to trigger on a 'push' event.
  name: hello-world-run
  annotations:
    pipelinesascode.tekton.dev/on-event: "[push]"
spec:
  pipelineRef:
    name: hello-world-pipeline # Reference the pipeline we created in Step 1
  params:
    # These values are automatically provided by the PaC controller
    # during the run, based on the git push event.
    - name: message
      value: "TODO: add overlays for custom msg per region"