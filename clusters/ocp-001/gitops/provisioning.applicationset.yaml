apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ocp-001-provisioning
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  generators:
  - list:
      elements:
      - component: cluster
        path: clusters/ocp-001/cluster
        destination: https://kubernetes.default.svc
        syncWave: "10"
  
  template:
    metadata:
      name: ocp-001-{{component}}
      namespace: openshift-gitops
      annotations:
        argocd.argoproj.io/sync-wave: '{{syncWave}}'
      labels:
        cluster: ocp-001
        phase: provisioning
    spec:
      project: default
      source:
        repoURL: https://github.com/openshift-online/bootstrap-hyperfleet
        path: '{{path}}'
        targetRevision: main
      destination:
        server: '{{destination}}'
      syncPolicy:
        automated:
          selfHeal: true
          allowEmpty: false
        prune: false
