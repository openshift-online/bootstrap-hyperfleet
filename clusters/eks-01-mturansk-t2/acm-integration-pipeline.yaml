---
apiVersion: v1
kind: ConfigMap
metadata:
  name: klusterlet-crd-eks-01-mturansk-t2
  namespace: eks-01-mturansk-t2
data:
  klusterlet-crd.yaml: |
    apiVersion: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    metadata:
      annotations:
          {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{},"creationTimestamp":"2025-07-19T22:10:25Z","generation":1,"name":"klusterlets.operator.open-cluster-management.io","ownerReferences":[{"apiVersion":"work.open-cluster-management.io/v1","kind":"AppliedManifestWork","name":"0dae03d5867a066358318689195a2ee96803079bf46e37f4fd3def4a47173d85-local-cluster-klusterlet-crds","uid":"bc350eff-4e50-4e29-a76e-07ec0cdb3359"}],"resourceVersion":"4383805","uid":"0acbaa91-53b6-47f2-9c24-aa37725cd8e0"},"spec":{"conversion":{"strategy":"None"},"group":"operator.open-cluster-management.io","names":{"kind":"Klusterlet","listKind":"KlusterletList","plural":"klusterlets","singular":"klusterlet"},"scope":"Cluster","versions":[{"name":"v1","schema":{"openAPIV3Schema":{"description":"Klusterlet represents controllers to install the resources for a managed cluster.\nWhen configured, the Klusterlet requires a secret named bootstrap-hub-kubeconfig in the\nagent namespace to allow API requests to the hub for the registration protocol.\nIn Hosted mode, the Klusterlet requires an additional secret named external-managed-kubeconfig\nin the agent namespace to allow API requests to the managed cluster for resources installation.","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"Spec represents the desired deployment configuration of Klusterlet agent.","properties":{"clusterName":{"description":"ClusterName is the name of the managed cluster to be created on hub.\nThe Klusterlet agent generates a random name if it is not set, or discovers the appropriate cluster name on OpenShift.","maxLength":63,"pattern":"^[a-z0-9]([-a-z0-9]*[a-z0-9])?$","type":"string"},"deployOption":{"description":"DeployOption contains the options of deploying a klusterlet","properties":{"mode":{"description":"Mode can be Default, Hosted, Singleton or SingletonHosted. It is Default mode if not specified\nIn Default mode, all klusterlet related resources are deployed on the managed cluster.\nIn Hosted mode, only crd and configurations are installed on the spoke/managed cluster. Controllers run in another\ncluster (defined as management-cluster) and connect to the mangaged cluster with the kubeconfig in secret of\n\"external-managed-kubeconfig\"(a kubeconfig of managed-cluster with cluster-admin permission).\nIn Singleton mode, registration/work agent is started as a single deployment.\nIn SingletonHosted mode, agent is started as a single deployment in hosted mode.\nNote: Do not modify the Mode field once it's applied.","type":"string"}},"type":"object"},"externalServerURLs":{"description":"ExternalServerURLs represents a list of apiserver urls and ca bundles that is accessible externally\nIf it is set empty, managed cluster has no externally accessible url that hub cluster can visit.","items":{"description":"ServerURL represents the apiserver url and ca bundle that is accessible externally","properties":{"caBundle":{"description":"CABundle is the ca bundle to connect to apiserver of the managed cluster.\nSystem certs are used if it is not set.","format":"byte","type":"string"},"url":{"description":"URL is the url of apiserver endpoint of the managed cluster.","type":"string"}},"type":"object"},"type":"array"},"hubApiServerHostAlias":{"description":"HubApiServerHostAlias contains the host alias for hub api server.\nregistration-agent and work-agent will use it to communicate with hub api server.","properties":{"hostname":{"description":"Hostname for the above IP address.","pattern":"^(([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9\\-]*[a-zA-Z0-9])\\.)*([A-Za-z0-9]|[A-Za-z0-9][A-Za-z0-9\\-]*[A-Za-z0-9])$","type":"string"},"ip":{"description":"IP address of the host file entry.","pattern":"^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$","type":"string"}},"required":["hostname","ip"],"type":"object"},"imagePullSpec":{"description":"ImagePullSpec represents the desired image configuration of agent, it takes effect only when\nsingleton mode is set. quay.io/open-cluster-management.io/registration-operator:latest will\nbe used if unspecified","type":"string"},"namespace":{"description":"Namespace is the namespace to deploy the agent on the managed cluster.\nThe namespace must have a prefix of \"open-cluster-management-\", and if it is not set,\nthe namespace of \"open-cluster-management-agent\" is used to deploy agent.\nIn addition, the add-ons are deployed to the namespace of \"{Namespace}-addon\".\nIn the Hosted mode, this namespace still exists on the managed cluster to contain\nnecessary resources, like service accounts, roles and rolebindings, while the agent\nis deployed to the namespace with the same name as klusterlet on the management cluster.","maxLength":57,"pattern":"^open-cluster-management-[-a-z0-9]*[a-z0-9]$","type":"string"},"nodePlacement":{"description":"NodePlacement enables explicit control over the scheduling of the deployed pods.","properties":{"nodeSelector":{"additionalProperties":{"type":"string"},"description":"NodeSelector defines which Nodes the Pods are scheduled on. The default is an empty list.","type":"object"},"tolerations":{"description":"Tolerations are attached by pods to tolerate any taint that matches\nthe triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.\nThe default is an empty list.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches\nthe triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects.\nWhen specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys.\nIf the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value.\nValid operators are Exists and Equal. Defaults to Equal.\nExists is equivalent to wildcard for value, so that a pod can\ntolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be\nof effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,\nit is not set, which means tolerate the taint forever (do not evict). Zero and\nnegative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to.\nIf the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"}},"type":"object"},"priorityClassName":{"description":"PriorityClassName is the name of the PriorityClass that will be used by the\ndeployed klusterlet agent. It will be ignored when the PriorityClass/v1 API\nis not available on the managed cluster.","type":"string"},"registrationConfiguration":{"description":"RegistrationConfiguration contains the configuration of registration","properties":{"bootstrapKubeConfigs":{"description":"BootstrapKubeConfigs defines the ordered list of bootstrap kubeconfigs. The order decides which bootstrap kubeconfig to use first when rebootstrap.\n\n\nWhen the agent loses the connection to the current hub over HubConnectionTimeoutSeconds, or the managedcluster CR\nis set `hubAcceptsClient=false` on the hub, the controller marks the related bootstrap kubeconfig as \"failed\".\n\n\nA failed bootstrapkubeconfig won't be used for the duration specified by SkipFailedBootstrapKubeConfigSeconds.\nBut if the user updates the content of a failed bootstrapkubeconfig, the \"failed\" mark will be cleared.","properties":{"localSecretsConfig":{"description":"LocalSecretsConfig include a list of secrets that contains the kubeconfigs for ordered bootstrap kubeconifigs.\nThe secrets must be in the same namespace where the agent controller runs.","properties":{"hubConnectionTimeoutSeconds":{"default":600,"description":"HubConnectionTimeoutSeconds is used to set the timeout of connecting to the hub cluster.\nWhen agent loses the connection to the hub over the timeout seconds, the agent do a rebootstrap.\nBy default is 10 mins.","format":"int32","minimum":180,"type":"integer"},"kubeConfigSecrets":{"description":"KubeConfigSecrets is a list of secret names. The secrets are in the same namespace where the agent controller runs.","items":{"properties":{"name":{"description":"Name is the name of the secret.","type":"string"}},"type":"object"},"type":"array"}},"type":"object"},"type":{"default":"None","description":"Type specifies the type of priority bootstrap kubeconfigs.\nBy default, it is set to None, representing no priority bootstrap kubeconfigs are set.","enum":["None","LocalSecrets"],"type":"string"}},"type":"object"},"clientCertExpirationSeconds":{"description":"clientCertExpirationSeconds represents the seconds of a client certificate to expire. If it is not set or 0, the default\nduration seconds will be set by the hub cluster. If the value is larger than the max signing duration seconds set on\nthe hub cluster, the max signing duration seconds will be set.","format":"int32","type":"integer"},"clusterAnnotations":{"additionalProperties":{"type":"string"},"description":"ClusterAnnotations is annotations with the reserve prefix \"agent.open-cluster-management.io\" set on\nManagedCluster when creating only, other actors can update it afterwards.","type":"object"},"featureGates":{"description":"FeatureGates represents the list of feature gates for registration\nIf it is set empty, default feature gates will be used.\nIf it is set, featuregate/Foo is an example of one item in FeatureGates:\n  1. If featuregate/Foo does not exist, registration-operator will discard it\n  2. If featuregate/Foo exists and is false by default. It is now possible to set featuregate/Foo=[false|true]\n  3. If featuregate/Foo exists and is true by default. If a cluster-admin upgrading from 1 to 2 wants to continue having featuregate/Foo=false,\n \the can set featuregate/Foo=false before upgrading. Let's say the cluster-admin wants featuregate/Foo=false.","items":{"properties":{"feature":{"description":"Feature is the key of feature gate. e.g. featuregate/Foo.","type":"string"},"mode":{"default":"Disable","description":"Mode is either Enable, Disable, \"\" where \"\" is Disable by default.\nIn Enable mode, a valid feature gate `featuregate/Foo` will be set to \"--featuregate/Foo=true\".\nIn Disable mode, a valid feature gate `featuregate/Foo` will be set to \"--featuregate/Foo=false\".","enum":["Enable","Disable"],"type":"string"}},"required":["feature"],"type":"object"},"type":"array"},"kubeAPIBurst":{"default":100,"description":"KubeAPIBurst indicates the maximum burst of the throttle while talking with apiserver of hub cluster from the spoke cluster.\nIf it is set empty, use the default value: 100","format":"int32","type":"integer"},"kubeAPIQPS":{"default":50,"description":"KubeAPIQPS indicates the maximum QPS while talking with apiserver of hub cluster from the spoke cluster.\nIf it is set empty, use the default value: 50","format":"int32","type":"integer"}},"type":"object"},"registrationImagePullSpec":{"description":"RegistrationImagePullSpec represents the desired image configuration of registration agent.\nquay.io/open-cluster-management.io/registration:latest will be used if unspecified.","type":"string"},"resourceRequirement":{"description":"ResourceRequirement specify QoS classes of deployments managed by klusterlet.\nIt applies to all the containers in the deployments.","properties":{"resourceRequirements":{"description":"ResourceRequirements defines resource requests and limits when Type is ResourceQosClassResourceRequirement","properties":{"claims":{"description":"Claims lists the names of resources, defined in spec.resourceClaims,\nthat are used by this container.\n\n\nThis is an alpha field and requires enabling the\nDynamicResourceAllocation feature gate.\n\n\nThis field is immutable. It can only be set for containers.","items":{"description":"ResourceClaim references one entry in PodSpec.ResourceClaims.","properties":{"name":{"description":"Name must match the name of one entry in pod.spec.resourceClaims of\nthe Pod where this field is used. It makes that resource available\ninside a container.","type":"string"}},"required":["name"],"type":"object"},"type":"array","x-kubernetes-list-map-keys":["name"],"x-kubernetes-list-type":"map"},"limits":{"additionalProperties":{"anyOf":[{"type":"integer"},{"type":"string"}],"pattern":"^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$","x-kubernetes-int-or-string":true},"description":"Limits describes the maximum amount of compute resources allowed.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/","type":"object"},"requests":{"additionalProperties":{"anyOf":[{"type":"integer"},{"type":"string"}],"pattern":"^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$","x-kubernetes-int-or-string":true},"description":"Requests describes the minimum amount of compute resources required.\nIf Requests is omitted for a container, it defaults to Limits if that is explicitly specified,\notherwise to an implementation-defined value. Requests cannot exceed Limits.\nMore info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/","type":"object"}},"type":"object"},"type":{"default":"Default","enum":["Default","BestEffort","ResourceRequirement"],"type":"string"}},"type":"object"},"workConfiguration":{"description":"WorkConfiguration contains the configuration of work","properties":{"appliedManifestWorkEvictionGracePeriod":{"description":"AppliedManifestWorkEvictionGracePeriod is the eviction grace period the work agent will wait before\nevicting the AppliedManifestWorks, whose corresponding ManifestWorks are missing on the hub cluster, from\nthe managed cluster. If not present, the default value of the work agent will be used.","pattern":"^([0-9]+(s|m|h))+$","type":"string"},"featureGates":{"description":"FeatureGates represents the list of feature gates for work\nIf it is set empty, default feature gates will be used.\nIf it is set, featuregate/Foo is an example of one item in FeatureGates:\n  1. If featuregate/Foo does not exist, registration-operator will discard it\n  2. If featuregate/Foo exists and is false by default. It is now possible to set featuregate/Foo=[false|true]\n  3. If featuregate/Foo exists and is true by default. If a cluster-admin upgrading from 1 to 2 wants to continue having featuregate/Foo=false,\n \the can set featuregate/Foo=false before upgrading. Let's say the cluster-admin wants featuregate/Foo=false.","items":{"properties":{"feature":{"description":"Feature is the key of feature gate. e.g. featuregate/Foo.","type":"string"},"mode":{"default":"Disable","description":"Mode is either Enable, Disable, \"\" where \"\" is Disable by default.\nIn Enable mode, a valid feature gate `featuregate/Foo` will be set to \"--featuregate/Foo=true\".\nIn Disable mode, a valid feature gate `featuregate/Foo` will be set to \"--featuregate/Foo=false\".","enum":["Enable","Disable"],"type":"string"}},"required":["feature"],"type":"object"},"type":"array"},"kubeAPIBurst":{"default":100,"description":"KubeAPIBurst indicates the maximum burst of the throttle while talking with apiserver of hub cluster from the spoke cluster.\nIf it is set empty, use the default value: 100","format":"int32","type":"integer"},"kubeAPIQPS":{"default":50,"description":"KubeAPIQPS indicates the maximum QPS while talking with apiserver of hub cluster from the spoke cluster.\nIf it is set empty, use the default value: 50","format":"int32","type":"integer"}},"type":"object"},"workImagePullSpec":{"description":"WorkImagePullSpec represents the desired image configuration of work agent.\nquay.io/open-cluster-management.io/work:latest will be used if unspecified.","type":"string"}},"type":"object"},"status":{"description":"Status represents the current status of Klusterlet agent.","properties":{"conditions":{"description":"Conditions contain the different condition statuses for this Klusterlet.\nValid condition types are:\nApplied: Components have been applied in the managed cluster.\nAvailable: Components in the managed cluster are available and ready to serve.\nProgressing: Components in the managed cluster are in a transitioning state.\nDegraded: Components in the managed cluster do not match the desired configuration and only provide\ndegraded service.","items":{"description":"Condition contains details for one aspect of the current state of this API Resource.\n---\nThis struct is intended for direct use as an array at the field path .status.conditions.  For example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the observations of a foo's current state.\n\t    // Known .status.conditions.type are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    // +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t    // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t    // other fields\n\t}","properties":{"lastTransitionTime":{"description":"lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.","format":"date-time","type":"string"},"message":{"description":"message is a human readable message indicating details about the transition.\nThis may be an empty string.","maxLength":32768,"type":"string"},"observedGeneration":{"description":"observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.","format":"int64","minimum":0,"type":"integer"},"reason":{"description":"reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.","maxLength":1024,"minLength":1,"pattern":"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$","type":"string"},"status":{"description":"status of the condition, one of True, False, Unknown.","enum":["True","False","Unknown"],"type":"string"},"type":{"description":"type of condition in CamelCase or in foo.example.com/CamelCase.\n---\nMany .condition.type values are consistent across resources like Available, but because arbitrary conditions can be\nuseful (see .node.status.conditions), the ability to deconflict is important.\nThe regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)","maxLength":316,"pattern":"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$","type":"string"}},"required":["lastTransitionTime","message","reason","status","type"],"type":"object"},"type":"array"},"generations":{"description":"Generations are used to determine when an item needs to be reconciled or has changed in a way that needs a reaction.","items":{"description":"GenerationStatus keeps track of the generation for a given resource so that decisions about forced updates can be made.\nThe definition matches the GenerationStatus defined in github.com/openshift/api/v1","properties":{"group":{"description":"group is the group of the resource that you're tracking","type":"string"},"lastGeneration":{"description":"lastGeneration is the last generation of the resource that controller applies","format":"int64","type":"integer"},"name":{"description":"name is the name of the resource that you're tracking","type":"string"},"namespace":{"description":"namespace is where the resource that you're tracking is","type":"string"},"resource":{"description":"resource is the resource type of the resource that you're tracking","type":"string"},"version":{"description":"version is the version of the resource that you're tracking","type":"string"}},"type":"object"},"type":"array"},"observedGeneration":{"description":"ObservedGeneration is the last generation change you've dealt with","format":"int64","type":"integer"},"relatedResources":{"description":"RelatedResources are used to track the resources that are related to this Klusterlet.","items":{"description":"RelatedResourceMeta represents the resource that is managed by an operator","properties":{"group":{"description":"group is the group of the resource that you're tracking","type":"string"},"name":{"description":"name is the name of the resource that you're tracking","type":"string"},"namespace":{"description":"namespace is where the thing you're tracking is","type":"string"},"resource":{"description":"resource is the resource type of the resource that you're tracking","type":"string"},"version":{"description":"version is the version of the thing you're tracking","type":"string"}},"type":"object"},"type":"array"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]},"status":{"acceptedNames":{"kind":"Klusterlet","listKind":"KlusterletList","plural":"klusterlets","singular":"klusterlet"},"conditions":[{"lastTransitionTime":"2025-07-19T22:10:25Z","message":"no conflicts found","reason":"NoConflicts","status":"True","type":"NamesAccepted"},{"lastTransitionTime":"2025-07-19T22:10:25Z","message":"the initial names have been accepted","reason":"InitialNamesAccepted","status":"True","type":"Established"}],"storedVersions":["v1"]}}
      name: klusterlets.operator.open-cluster-management.io
      - apiVersion: work.open-cluster-management.io/v1
        kind: AppliedManifestWork
        name: 0dae03d5867a066358318689195a2ee96803079bf46e37f4fd3def4a47173d85-local-cluster-klusterlet-crds
    spec:
      conversion:
        strategy: None
      group: operator.open-cluster-management.io
      names:
        kind: Klusterlet
        listKind: KlusterletList
        plural: klusterlets
        singular: klusterlet
      scope: Cluster
      versions:
      - name: v1
        schema:
          openAPIV3Schema:
            description: |-
              Klusterlet represents controllers to install the resources for a managed cluster.
              When configured, the Klusterlet requires a secret named bootstrap-hub-kubeconfig in the
              agent namespace to allow API requests to the hub for the registration protocol.
              In Hosted mode, the Klusterlet requires an additional secret named external-managed-kubeconfig
              in the agent namespace to allow API requests to the managed cluster for resources installation.
            properties:
              apiVersion:
                description: |-
                  APIVersion defines the versioned schema of this representation of an object.
                  Servers should convert recognized schemas to the latest internal value, and
                  may reject unrecognized values.
                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                type: string
              kind:
                description: |-
                  Kind is a string value representing the REST resource this object represents.
                  Servers may infer this from the endpoint the client submits requests to.
                  Cannot be updated.
                  In CamelCase.
                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                type: string
              metadata:
                type: object
              spec:
                description: Spec represents the desired deployment configuration of Klusterlet
                  agent.
                properties:
                  clusterName:
                    description: |-
                      ClusterName is the name of the managed cluster to be created on hub.
                      The Klusterlet agent generates a random name if it is not set, or discovers the appropriate cluster name on OpenShift.
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  deployOption:
                    description: DeployOption contains the options of deploying a klusterlet
                    properties:
                      mode:
                        description: |-
                          Mode can be Default, Hosted, Singleton or SingletonHosted. It is Default mode if not specified
                          In Default mode, all klusterlet related resources are deployed on the managed cluster.
                          In Hosted mode, only crd and configurations are installed on the spoke/managed cluster. Controllers run in another
                          cluster (defined as management-cluster) and connect to the mangaged cluster with the kubeconfig in secret of
                          "external-managed-kubeconfig"(a kubeconfig of managed-cluster with cluster-admin permission).
                          In Singleton mode, registration/work agent is started as a single deployment.
                          In SingletonHosted mode, agent is started as a single deployment in hosted mode.
                          Note: Do not modify the Mode field once it's applied.
                        type: string
                    type: object
                  externalServerURLs:
                    description: |-
                      ExternalServerURLs represents a list of apiserver urls and ca bundles that is accessible externally
                      If it is set empty, managed cluster has no externally accessible url that hub cluster can visit.
                    items:
                      description: ServerURL represents the apiserver url and ca bundle
                        that is accessible externally
                      properties:
                        caBundle:
                          description: |-
                            CABundle is the ca bundle to connect to apiserver of the managed cluster.
                            System certs are used if it is not set.
                          format: byte
                          type: string
                        url:
                          description: URL is the url of apiserver endpoint of the managed
                            cluster.
                          type: string
                      type: object
                    type: array
                  hubApiServerHostAlias:
                    description: |-
                      HubApiServerHostAlias contains the host alias for hub api server.
                      registration-agent and work-agent will use it to communicate with hub api server.
                    properties:
                      hostname:
                        description: Hostname for the above IP address.
                        pattern: ^(([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9\-]*[a-zA-Z0-9])\.)*([A-Za-z0-9]|[A-Za-z0-9][A-Za-z0-9\-]*[A-Za-z0-9])$
                        type: string
                      ip:
                        description: IP address of the host file entry.
                        pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$
                        type: string
                    required:
                    - hostname
                    - ip
                    type: object
                  imagePullSpec:
                    description: |-
                      ImagePullSpec represents the desired image configuration of agent, it takes effect only when
                      singleton mode is set. quay.io/open-cluster-management.io/registration-operator:latest will
                      be used if unspecified
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace to deploy the agent on the managed cluster.
                      The namespace must have a prefix of "open-cluster-management-", and if it is not set,
                      the namespace of "open-cluster-management-agent" is used to deploy agent.
                      In addition, the add-ons are deployed to the namespace of "{Namespace}-addon".
                      In the Hosted mode, this namespace still exists on the managed cluster to contain
                      necessary resources, like service accounts, roles and rolebindings, while the agent
                      is deployed to the namespace with the same name as klusterlet on the management cluster.
                    maxLength: 57
                    pattern: ^open-cluster-management-[-a-z0-9]*[a-z0-9]$
                    type: string
                  nodePlacement:
                    description: NodePlacement enables explicit control over the scheduling
                      of the deployed pods.
                    properties:
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector defines which Nodes the Pods are scheduled
                          on. The default is an empty list.
                        type: object
                      tolerations:
                        description: |-
                          Tolerations are attached by pods to tolerate any taint that matches
                          the triple <key,value,effect> using the matching operator <operator>.
                          The default is an empty list.
                        items:
                          description: |-
                            The pod this Toleration is attached to tolerates any taint that matches
                            the triple <key,value,effect> using the matching operator <operator>.
                          properties:
                            effect:
                              description: |-
                                Effect indicates the taint effect to match. Empty means match all taint effects.
                                When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                              type: string
                            key:
                              description: |-
                                Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                              type: string
                            operator:
                              description: |-
                                Operator represents a key's relationship to the value.
                                Valid operators are Exists and Equal. Defaults to Equal.
                                Exists is equivalent to wildcard for value, so that a pod can
                                tolerate all taints of a particular category.
                              type: string
                            tolerationSeconds:
                              description: |-
                                TolerationSeconds represents the period of time the toleration (which must be
                                of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                it is not set, which means tolerate the taint forever (do not evict). Zero and
                                negative values will be treated as 0 (evict immediately) by the system.
                              format: int64
                              type: integer
                            value:
                              description: |-
                                Value is the taint value the toleration matches to.
                                If the operator is Exists, the value should be empty, otherwise just a regular string.
                              type: string
                          type: object
                        type: array
                    type: object
                  priorityClassName:
                    description: |-
                      PriorityClassName is the name of the PriorityClass that will be used by the
                      deployed klusterlet agent. It will be ignored when the PriorityClass/v1 API
                      is not available on the managed cluster.
                    type: string
                  registrationConfiguration:
                    description: RegistrationConfiguration contains the configuration
                      of registration
                    properties:
                      bootstrapKubeConfigs:
                        description: |-
                          BootstrapKubeConfigs defines the ordered list of bootstrap kubeconfigs. The order decides which bootstrap kubeconfig to use first when rebootstrap.
    
    
                          When the agent loses the connection to the current hub over HubConnectionTimeoutSeconds, or the managedcluster CR
                          is set `hubAcceptsClient=false` on the hub, the controller marks the related bootstrap kubeconfig as "failed".
    
    
                          A failed bootstrapkubeconfig won't be used for the duration specified by SkipFailedBootstrapKubeConfigSeconds.
                          But if the user updates the content of a failed bootstrapkubeconfig, the "failed" mark will be cleared.
                        properties:
                          localSecretsConfig:
                            description: |-
                              LocalSecretsConfig include a list of secrets that contains the kubeconfigs for ordered bootstrap kubeconifigs.
                              The secrets must be in the same namespace where the agent controller runs.
                            properties:
                              hubConnectionTimeoutSeconds:
                                default: 600
                                description: |-
                                  HubConnectionTimeoutSeconds is used to set the timeout of connecting to the hub cluster.
                                  When agent loses the connection to the hub over the timeout seconds, the agent do a rebootstrap.
                                  By default is 10 mins.
                                format: int32
                                minimum: 180
                                type: integer
                              kubeConfigSecrets:
                                description: KubeConfigSecrets is a list of secret names.
                                  The secrets are in the same namespace where the agent
                                  controller runs.
                                items:
                                  properties:
                                    name:
                                      description: Name is the name of the secret.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type:
                            default: None
                            description: |-
                              Type specifies the type of priority bootstrap kubeconfigs.
                              By default, it is set to None, representing no priority bootstrap kubeconfigs are set.
                            enum:
                            - None
                            - LocalSecrets
                            type: string
                        type: object
                      clientCertExpirationSeconds:
                        description: |-
                          clientCertExpirationSeconds represents the seconds of a client certificate to expire. If it is not set or 0, the default
                          duration seconds will be set by the hub cluster. If the value is larger than the max signing duration seconds set on
                          the hub cluster, the max signing duration seconds will be set.
                        format: int32
                        type: integer
                      clusterAnnotations:
                        additionalProperties:
                          type: string
                        description: |-
                          ClusterAnnotations is annotations with the reserve prefix "agent.open-cluster-management.io" set on
                          ManagedCluster when creating only, other actors can update it afterwards.
                        type: object
                      featureGates:
                        description: "FeatureGates represents the list of feature gates
                          for registration\nIf it is set empty, default feature gates
                          will be used.\nIf it is set, featuregate/Foo is an example of
                          one item in FeatureGates:\n  1. If featuregate/Foo does not
                          exist, registration-operator will discard it\n  2. If featuregate/Foo
                          exists and is false by default. It is now possible to set featuregate/Foo=[false|true]\n
                          \ 3. If featuregate/Foo exists and is true by default. If a
                          cluster-admin upgrading from 1 to 2 wants to continue having
                          featuregate/Foo=false,\n \the can set featuregate/Foo=false
                          before upgrading. Let's say the cluster-admin wants featuregate/Foo=false."
                        items:
                          properties:
                            feature:
                              description: Feature is the key of feature gate. e.g. featuregate/Foo.
                              type: string
                            mode:
                              default: Disable
                              description: |-
                                Mode is either Enable, Disable, "" where "" is Disable by default.
                                In Enable mode, a valid feature gate `featuregate/Foo` will be set to "--featuregate/Foo=true".
                                In Disable mode, a valid feature gate `featuregate/Foo` will be set to "--featuregate/Foo=false".
                              enum:
                              - Enable
                              - Disable
                              type: string
                          required:
                          - feature
                          type: object
                        type: array
                      kubeAPIBurst:
                        default: 100
                        description: |-
                          KubeAPIBurst indicates the maximum burst of the throttle while talking with apiserver of hub cluster from the spoke cluster.
                          If it is set empty, use the default value: 100
                        format: int32
                        type: integer
                      kubeAPIQPS:
                        default: 50
                        description: |-
                          KubeAPIQPS indicates the maximum QPS while talking with apiserver of hub cluster from the spoke cluster.
                          If it is set empty, use the default value: 50
                        format: int32
                        type: integer
                    type: object
                  registrationImagePullSpec:
                    description: |-
                      RegistrationImagePullSpec represents the desired image configuration of registration agent.
                      quay.io/open-cluster-management.io/registration:latest will be used if unspecified.
                    type: string
                  resourceRequirement:
                    description: |-
                      ResourceRequirement specify QoS classes of deployments managed by klusterlet.
                      It applies to all the containers in the deployments.
                    properties:
                      resourceRequirements:
                        description: ResourceRequirements defines resource requests and
                          limits when Type is ResourceQosClassResourceRequirement
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.
    
    
                              This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate.
    
    
                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      type:
                        default: Default
                        enum:
                        - Default
                        - BestEffort
                        - ResourceRequirement
                        type: string
                    type: object
                  workConfiguration:
                    description: WorkConfiguration contains the configuration of work
                    properties:
                      appliedManifestWorkEvictionGracePeriod:
                        description: |-
                          AppliedManifestWorkEvictionGracePeriod is the eviction grace period the work agent will wait before
                          evicting the AppliedManifestWorks, whose corresponding ManifestWorks are missing on the hub cluster, from
                          the managed cluster. If not present, the default value of the work agent will be used.
                        pattern: ^([0-9]+(s|m|h))+$
                        type: string
                      featureGates:
                        description: "FeatureGates represents the list of feature gates
                          for work\nIf it is set empty, default feature gates will be
                          used.\nIf it is set, featuregate/Foo is an example of one item
                          in FeatureGates:\n  1. If featuregate/Foo does not exist, registration-operator
                          will discard it\n  2. If featuregate/Foo exists and is false
                          by default. It is now possible to set featuregate/Foo=[false|true]\n
                          \ 3. If featuregate/Foo exists and is true by default. If a
                          cluster-admin upgrading from 1 to 2 wants to continue having
                          featuregate/Foo=false,\n \the can set featuregate/Foo=false
                          before upgrading. Let's say the cluster-admin wants featuregate/Foo=false."
                        items:
                          properties:
                            feature:
                              description: Feature is the key of feature gate. e.g. featuregate/Foo.
                              type: string
                            mode:
                              default: Disable
                              description: |-
                                Mode is either Enable, Disable, "" where "" is Disable by default.
                                In Enable mode, a valid feature gate `featuregate/Foo` will be set to "--featuregate/Foo=true".
                                In Disable mode, a valid feature gate `featuregate/Foo` will be set to "--featuregate/Foo=false".
                              enum:
                              - Enable
                              - Disable
                              type: string
                          required:
                          - feature
                          type: object
                        type: array
                      kubeAPIBurst:
                        default: 100
                        description: |-
                          KubeAPIBurst indicates the maximum burst of the throttle while talking with apiserver of hub cluster from the spoke cluster.
                          If it is set empty, use the default value: 100
                        format: int32
                        type: integer
                      kubeAPIQPS:
                        default: 50
                        description: |-
                          KubeAPIQPS indicates the maximum QPS while talking with apiserver of hub cluster from the spoke cluster.
                          If it is set empty, use the default value: 50
                        format: int32
                        type: integer
                    type: object
                  workImagePullSpec:
                    description: |-
                      WorkImagePullSpec represents the desired image configuration of work agent.
                      quay.io/open-cluster-management.io/work:latest will be used if unspecified.
                    type: string
                type: object
              status:
                description: Status represents the current status of Klusterlet agent.
                properties:
                  conditions:
                    description: |-
                      Conditions contain the different condition statuses for this Klusterlet.
                      Valid condition types are:
                      Applied: Components have been applied in the managed cluster.
                      Available: Components in the managed cluster are available and ready to serve.
                      Progressing: Components in the managed cluster are in a transitioning state.
                      Degraded: Components in the managed cluster do not match the desired configuration and only provide
                      degraded service.
                    items:
                      description: "Condition contains details for one aspect of the current
                        state of this API Resource.\n---\nThis struct is intended for
                        direct use as an array at the field path .status.conditions.  For
                        example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                        observations of a foo's current state.\n\t    // Known .status.conditions.type
                        are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                        +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                        \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                        patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                        \   // other fields\n\t}"
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False, Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: |-
                            type of condition in CamelCase or in foo.example.com/CamelCase.
                            ---
                            Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                            useful (see .node.status.conditions), the ability to deconflict is important.
                            The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                  generations:
                    description: Generations are used to determine when an item needs
                      to be reconciled or has changed in a way that needs a reaction.
                    items:
                      description: |-
                        GenerationStatus keeps track of the generation for a given resource so that decisions about forced updates can be made.
                        The definition matches the GenerationStatus defined in github.com/openshift/api/v1
                      properties:
                        group:
                          description: group is the group of the resource that you're
                            tracking
                          type: string
                        lastGeneration:
                          description: lastGeneration is the last generation of the resource
                            that controller applies
                          format: int64
                          type: integer
                        name:
                          description: name is the name of the resource that you're tracking
                          type: string
                        namespace:
                          description: namespace is where the resource that you're tracking
                            is
                          type: string
                        resource:
                          description: resource is the resource type of the resource that
                            you're tracking
                          type: string
                        version:
                          description: version is the version of the resource that you're
                            tracking
                          type: string
                      type: object
                    type: array
                  observedGeneration:
                    description: ObservedGeneration is the last generation change you've
                      dealt with
                    format: int64
                    type: integer
                  relatedResources:
                    description: RelatedResources are used to track the resources that
                      are related to this Klusterlet.
                    items:
                      description: RelatedResourceMeta represents the resource that is
                        managed by an operator
                      properties:
                        group:
                          description: group is the group of the resource that you're
                            tracking
                          type: string
                        name:
                          description: name is the name of the resource that you're tracking
                          type: string
                        namespace:
                          description: namespace is where the thing you're tracking is
                          type: string
                        resource:
                          description: resource is the resource type of the resource that
                            you're tracking
                          type: string
                        version:
                          description: version is the version of the thing you're tracking
                          type: string
                      type: object
                    type: array
                type: object
            type: object
        served: true
        storage: true
        subresources:
          status: {}
    status:
      acceptedNames:
        kind: Klusterlet
        listKind: KlusterletList
        plural: klusterlets
        singular: klusterlet
      conditions:
      - lastTransitionTime: "2025-07-19T22:10:25Z"
        message: no conflicts found
        reason: NoConflicts
        status: "True"
        type: NamesAccepted
      - lastTransitionTime: "2025-07-19T22:10:25Z"
        message: the initial names have been accepted
        reason: InitialNamesAccepted
        status: "True"
        type: Established
      storedVersions:
      - v1
---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: eks-acm-integration-eks-01-mturansk-t2
  namespace: eks-01-mturansk-t2
spec:
  params:
  - name: cluster-name
    type: string
    default: "eks-01-mturansk-t2"
  - name: region
    type: string  
    default: "us-west-2"
  stepTemplate:
    env:
    - name: AWS_DEFAULT_REGION
      value: $(params.region)
    volumeMounts:
    - name: aws-credentials
      mountPath: /root/.aws
      readOnly: true
  steps:
  - name: install-tools
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      echo "Installing required tools..."
      
      # Install kubectl
      curl -LO "https://dl.k8s.io/release/v1.28.0/bin/linux/amd64/kubectl"
      chmod +x kubectl && mv kubectl /usr/local/bin/
      
      # Install AWS CLI
      curl -LO "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
      unzip awscli-exe-linux-x86_64.zip && ./aws/install
      
      # Install OpenShift CLI
      curl -L "https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz" | tar -xz -C /usr/local/bin/ oc
      
      echo "Tools installed successfully"
      
  - name: wait-for-eks-cluster
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      
      CLUSTER_NAME="$(params.cluster-name)"
      REGION="$(params.region)"
      
      echo "Waiting for EKS cluster $CLUSTER_NAME to become ACTIVE..."
      
      TIMEOUT=1800  # 30 minutes
      ELAPSED=0
      while [ $ELAPSED -lt $TIMEOUT ]; do
        STATUS=$(aws eks describe-cluster --name $CLUSTER_NAME --region $REGION --query 'cluster.status' --output text 2>/dev/null || echo "CREATING")
        echo "EKS cluster status: $STATUS (elapsed: ${ELAPSED}s)"
        
        if [ "$STATUS" = "ACTIVE" ]; then
          echo "EKS cluster is ACTIVE"
          break
        fi
        
        sleep 30
        ELAPSED=$((ELAPSED + 30))
      done
      
      if [ $ELAPSED -ge $TIMEOUT ]; then
        echo "ERROR: EKS cluster did not become ACTIVE within 30 minutes"
        exit 1
      fi
      
  - name: configure-managed-cluster
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      
      CLUSTER_NAME="$(params.cluster-name)"
      REGION="$(params.region)"
      
      echo "Configuring access to managed cluster..."
      aws eks update-kubeconfig --name $CLUSTER_NAME --region $REGION --kubeconfig /tmp/managed-kubeconfig
      
      echo "Installing Klusterlet CRD on managed cluster..."
      export KUBECONFIG=/tmp/managed-kubeconfig
      kubectl apply -f /workspace/klusterlet-crd/klusterlet-crd.yaml
      
      echo "Waiting for EKS worker nodes to be ready..."
      kubectl wait --for=condition=Ready nodes --all --timeout=600s
      
    volumeMounts:
    - name: klusterlet-crd
      mountPath: /workspace/klusterlet-crd
      
  - name: apply-acm-import
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      
      CLUSTER_NAME="$(params.cluster-name)"
      REGION="$(params.region)"
      
      echo "Extracting ACM import manifest from hub cluster..."
      # Use hub cluster service account token
      export KUBECONFIG=/var/run/secrets/kubernetes.io/serviceaccount/token
      oc get secret $CLUSTER_NAME-import -n $CLUSTER_NAME -o jsonpath='{.data.import\.yaml}' | base64 -d > /tmp/import.yaml
      
      echo "Applying ACM import manifest to managed cluster..."
      export KUBECONFIG=/tmp/managed-kubeconfig
      kubectl apply -f /tmp/import.yaml
      
      echo "Monitoring klusterlet deployment..."
      sleep 60
      
  - name: fix-pull-secret
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      
      CLUSTER_NAME="$(params.cluster-name)"
      export KUBECONFIG=/tmp/managed-kubeconfig
      
      if kubectl get pods -n open-cluster-management-agent 2>/dev/null | grep -q "ImagePullBackOff\|ErrImagePull"; then
        echo "Fixing image pull secret for klusterlet..."
        
        # Get pull secret from hub cluster
        export KUBECONFIG=/var/run/secrets/kubernetes.io/serviceaccount/token
        oc get secret pull-secret -n openshift-config -o yaml |           sed 's/namespace: openshift-config/namespace: open-cluster-management-agent/' |           sed 's/name: pull-secret/name: open-cluster-management-image-pull-credentials/' > /tmp/acm-pull-secret.yaml
        
        # Apply to managed cluster
        export KUBECONFIG=/tmp/managed-kubeconfig
        kubectl delete secret open-cluster-management-image-pull-credentials -n open-cluster-management-agent --ignore-not-found=true
        kubectl apply -f /tmp/acm-pull-secret.yaml
        kubectl rollout restart deployment/klusterlet -n open-cluster-management-agent
        
        echo "Pull secret fixed, waiting for klusterlet to restart..."
        sleep 60
      else
        echo "No image pull issues detected"
      fi
      
  - name: verify-acm-integration
    image: registry.redhat.io/ubi9/ubi:latest
    script: |
      #!/bin/bash
      set -e
      
      CLUSTER_NAME="$(params.cluster-name)"
      
      echo "Verifying ACM integration..."
      export KUBECONFIG=/var/run/secrets/kubernetes.io/serviceaccount/token
      
      TIMEOUT=600  # 10 minutes
      ELAPSED=0
      while [ $ELAPSED -lt $TIMEOUT ]; do
        STATUS=$(oc get managedcluster $CLUSTER_NAME -o jsonpath='{.status.conditions[?(@.type=="ManagedClusterConditionAvailable")].status}' 2>/dev/null || echo "Unknown")
        echo "ManagedCluster $CLUSTER_NAME availability status: $STATUS (elapsed: ${ELAPSED}s)"
        
        if [ "$STATUS" = "True" ]; then
          echo "SUCCESS: EKS cluster $CLUSTER_NAME successfully integrated with ACM"
          oc get managedcluster $CLUSTER_NAME
          exit 0
        fi
        
        sleep 30
        ELAPSED=$((ELAPSED + 30))
      done
      
      echo "WARNING: ACM integration did not complete within 10 minutes"
      echo "Current ManagedCluster status:"
      oc get managedcluster $CLUSTER_NAME -o yaml
      exit 0  # Don't fail the pipeline, cluster may still be integrating
      
  volumes:
  - name: aws-credentials
    secret:
      secretName: aws-credentials
  - name: klusterlet-crd
    configMap:
      name: klusterlet-crd-eks-01-mturansk-t2
---
apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: eks-01-mturansk-t2-acm-integration
  namespace: eks-01-mturansk-t2
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  pipelineSpec:
    tasks:
    - name: integrate-with-acm
      taskRef:
        name: eks-acm-integration-eks-01-mturansk-t2
  serviceAccountName: cluster-provisioner
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cluster-provisioner
  namespace: eks-01-mturansk-t2
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-provisioner-eks-01-mturansk-t2
rules:
- apiGroups: [""]
  resources: ["secrets", "configmaps"]
  verbs: ["get", "list", "create", "update", "patch", "delete"]
- apiGroups: ["cluster.open-cluster-management.io"]
  resources: ["managedclusters"]
  verbs: ["get", "list", "watch", "create", "update", "patch"]
- apiGroups: ["apiextensions.k8s.io"]
  resources: ["customresourcedefinitions"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cluster-provisioner-eks-01-mturansk-t2
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-provisioner-eks-01-mturansk-t2
subjects:
- kind: ServiceAccount
  name: cluster-provisioner
  namespace: eks-01-mturansk-t2
