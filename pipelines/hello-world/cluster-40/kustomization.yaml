apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ocm-cluster-40

commonAnnotations:
  cluster: cluster-40
  cluster-type: eks
  version: "v0.0.1"

resources:
  - ../../../operators/openshift-pipelines/operator/overlays/pipelines-operator-only
  - hello-world.pipelinerun.yaml

components:
  - ../../../bases/pipelines/hello-world
