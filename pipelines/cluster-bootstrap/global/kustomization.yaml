apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-pipelines

components:
  - ../../../bases/pipelines/cluster-bootstrap

resources:
  - cluster-bootstrap.pipelinerun.yaml

