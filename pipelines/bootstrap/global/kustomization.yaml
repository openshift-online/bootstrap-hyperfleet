apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bootstrap

components:
  - ../../../bases/pipelines/bootstrap

resources:
  - namespace.yaml
  - bootstrap.pipelinerun.yaml

