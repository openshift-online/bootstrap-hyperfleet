apiVersion: regional.openshift.io/v1
kind: RegionalCluster
metadata:
  name: ocp-01-hyp
  namespace: us-west-2
spec:
  type: ocp
  region: us-west-2
  domain: hyperfleet.bootstrap.red-chesterfield.com
  
  # Minimal compute config
  compute:
    instanceType: m5.2xlarge
    replicas: 2
    
  # Type-specific configuration
  openshift:
    version: "4.15"
    channel: stable
