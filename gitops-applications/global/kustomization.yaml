apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops

resources:
  - advanced-cluster-management/
  - openshift-pipelines-operator/
  - acm-gitops-integration/ # imports new managed clusters into argo
  - cluster-provisioning-metrics/ # monitors cluster provisioning
  - vault/
  - eso/
  - gitea/
  - cluster-bootstrap/
