apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster-specific applications that reference internal Gitea repository
# This creates self-referential configuration where the cluster manages itself
# via its own internal Git service populated by the bootstrap process

resources:
- clusters-applicationset.yaml












- ./ocp-4321.yaml
